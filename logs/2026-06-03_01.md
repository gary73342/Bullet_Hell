# 2026-06-03_01 遊戲層調整

## Bug 修復

- `reward.py`：補血獎勵注釋縮排錯誤，修正至正確層級
- `entities.py`：`Player.hitbox` 移至 `__init__` 初始化，避免第一幀前存取時 AttributeError
- `game.py`：敵方子彈命中玩家 hitbox 時，改為刪除所有命中子彈（原本只刪第一顆）

## Reward / Gym 修復

- 補血包碰撞偵測改用玩家 8×8 hitbox（原本用 36×40 完整 rect，與子彈碰撞邏輯不一致）
- 滿血吃到補血包不再給 `REWARD_HEAL`（改為比較 `old_hp`，HP 未實際增加則 `healed = False`）

## 敵機生成系統重構

- 移除舊的 `frame % spawn_interval` 固定間隔生成邏輯
- 改為 Formation 模式：從 14 種編隊模板隨機挑選（2～5 隻），每個 X 位置加 ±5% jitter
- 生成間隔改用 `uniform(spawn_interval * 0.5, spawn_interval * 1.5)`，避免指數分布長尾造成空檔
- `DRONE_SPAWN_INTERVAL` 從 90 → 70（生成頻率提升）
- 移除 6 隻大編隊模板（壓迫感過強）

## Drone 行為調整

- `wave_amplitude` 從 `randint(2, 5)` 縮小至 `randint(1, 3)`，左右晃動幅度降低

## Bug 修復（0603 補充）

- `game.py:194`：補血包撿取判定從 8×8 hitbox 改回 36×40 player rect
  - 原因：8×8 hitbox 過小導致補血包幾乎撿不到，REWARD_HEAL 形同虛設，影響訓練效果
  - 注意：與前一次改動（Reward/Gym 修復第一條）方向相反，確認以 rect 為準
- `game.py:146`：修正 HEAL_DROP_INTERVAL 註解錯誤（「每 10 秒 (600幀)」→「每 5 秒 (300幀)」，設定值 300 不變）

## 第一次訓練執行紀錄（1M steps）

### 訓練設定
- `TOTAL_TIMESTEPS = 1_000_000`
- `SAVE_FREQ = 100_000`
- PPO + CnnPolicy，`n_steps=2048`，`batch_size=64`，`n_epochs=10`，`lr=2.5e-4`
- 裝置：RTX 4060 Mobile（自動偵測 CUDA）
- 訓練時間：約 88 分鐘（5296 秒）
- 速度：約 188～214 steps/sec

### 最終指標
| 指標 | 開始 | 結束 |
|------|------|------|
| ep_rew_mean | ~198 | 336 |
| ep_len_mean | 872 | 899 |
| explained_variance | -0.071 | 0.578 |
| entropy_loss | -2.05 | -0.577 |
| value_loss | 25.7 | 9.93 |
| clip_fraction | 0.405 | 0.386 |

### 結論
- Reward 上升 70%，模型有在學習
- `approx_kl = 0.207`、`clip_fraction = 0.386` 偏高，尚未收斂
- 建議後續加跑 2～3M steps

## evaluate.py Bug 修復

- 加入 pygame 事件偵測（QUIT / ESC），讓關閉視窗能正常結束程式
- 加入 try/finally 確保 `env.close()` 一定被呼叫
- `env/bullet_hell_env.py:64`：`ACTION_MAP[action]` 改為 `ACTION_MAP[int(action)]`
  - 原因：`model.predict()` 回傳 `numpy.ndarray`，無法直接當 dict key
