# 2026-06-04_01 遊戲層調整

## 敵機行為調整

- `entities.py`：移除 Drone 左右正弦波搖擺移動（`wave_offset`、`wave_amplitude` 屬性與相關計算全部刪除）
- 敵機改為純直線向下飛行，保留隨機編隊生成邏輯

## 難度曲線調整

### 敵機生成間隔
- `settings.py`：`DRONE_SPAWN_INTERVAL` 70 → 72
- `game.py`：縮短速率從每級 `-4` 改為 `-3`，下限從 `50` 改為 `15`
- 新曲線：約第 19 級（3 分 10 秒）才到下限，後期壓力持續更久

### 敵機射擊間隔（動態化）
- `settings.py`：`DRONE_FIRE_INTERVAL` 固定 90 → 初始 `180`，新增 `DRONE_FIRE_INTERVAL_MIN = 45`
- `game.py`：`_level_params` 新增 `fire_interval = max(45, 180 - lv * 10)` 並傳入 Drone
- `entities.py`：`Drone.__init__` 新增 `fire_interval` 參數，`try_shoot` 改用 `self.fire_interval`
- 效果：前期每 3 秒/發，約第 13 級收緊至 0.75 秒/發

### 敵機血量（動態化）
- `game.py`：`_level_params` 新增 `drone_hp = min(3, 1 + lv // 8)` 並傳入 Drone
- `entities.py`：`Drone.__init__` 新增 `hp` 參數
- 血量進程：等級 0–7 → 1 血，8–15 → 2 血，16+ → 3 血

## 玩家升級邏輯調整

- `settings.py`：`PLAYER_MAX_LEVEL` 6 → 20
- `game.py`：射擊間隔每級從 `÷1.05` 改為 `-0.1 幀`，下限從 `2` 改為 `6`
- `game.py`：雙發解鎖從第 4 級延後至第 10 級
- 新曲線：等級 0 = 8.0 幀，等級 20 = 6.0 幀，線性平滑成長

## 玩家 Hitbox 調整

- `entities.py`：玩家 hitbox 從 `8×8` 擴大為 `32×8`
- 橫向幾乎貼齊戰機邊緣（戰機寬 36px），降低貼底角落存活的可能

## 敵方子彈調整

- `entities.py`：`EnemyBullet` 寬度從 `12` → `18` px，增加橫向覆蓋範圍

## 血包機制調整

- `settings.py`：`HEAL_ITEM_HP_RESTORE` 5 → 3
- `game.py`：等級 10 後判定間隔從 300 幀縮短為 180 幀（5 秒 → 3 秒）

## HUD 修復

- `game.py`：`P-LV` 文字改為靠右對齊（`right=SCREEN_WIDTH - 10`），修正等級 10+ 數字被截斷的問題

## Bug 修復

- `game.py`：修正雙發子彈同幀打中同一敵機導致計分與爆炸重複觸發的問題
  - 改為先以 `hit_drones` dict 累計每架敵機本幀受到的傷害，再統一結算
  - 影響場景：玩家等級 10+ 解鎖雙發後，兩顆子彈可能在同一幀命中同一架敵機
